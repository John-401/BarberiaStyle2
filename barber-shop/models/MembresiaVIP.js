import { ServicioBase } from './ServicioBase.js';
export class MembresiaVIP extends ServicioBase { constructor(c) { super("VIP", 120000); this.c = parseInt(c); } calcularInteres() { if (this.c <= 2) return 0; if (this.c <= 6) return 0.019; return 0.023; } calcularPrecio(c) { const i = this.calcularInteres(); const t = this.costoBase * (1 + (i * this.c)); return parseFloat((t / this.c).toFixed(2)); } procesarPago(c) { const cuota = this.calcularPrecio(c); if (c.saldo >= cuota) { c.descontarSaldo(cuota); return { success: true, method: `Cuota ${this.c}x ($${cuota})` }; } return { success: false, msg: "Insuficiente para 1ra cuota." }; } }
